# Updated Blueprints

This folder contains scenario blueprints that were automatically remapped to use freshly created Make connections. Each file mirrors the original blueprint name with an `-updated.json` suffix and has all non Facebook modules pointing to the new connection id.
